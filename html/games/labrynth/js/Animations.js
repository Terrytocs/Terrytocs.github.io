("use strict");

import Enums from "./Enum.js";

export default class Animations{
    static PlayerAnimationFrames=new Map([
        
    ]);

    static PlayerAnimations={
        
    };
}