("use strict");

export default class Idle{
    static methods={
        init_idle:this.init_idle,
        update_idle:this.update_idle,
        exit_idle:this.exit_idle
    }

    static init_idle(){

    };

    static update_idle(){

    };

    static exit_idle(){
        
    };
}